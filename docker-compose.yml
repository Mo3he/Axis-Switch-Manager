services:
  axis-switch-manager:
    image: ghcr.io/mo3he/axis-switch-manager:latest
    ports:
      - "8000:8000"
    volumes:
      - switches_data:/app/backend
    restart: unless-stopped

volumes:
  switches_data:
